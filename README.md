# README

# Job Portal Project

## Project Setup

- **Docker**: Install Docker Desktop (macOS/Windows) or Docker CLI (Linux).
```
  docker --version
  git clone
  cd job_portal
  docker-compose up --build
  docker-compose up
```